import numpy as np
import sympy as sp
import compFunc as cf
import matplotlib.pyplot as plt


def prob1():
    xStart = 0
    xStop = .5
    y = np.array([0.0, 1.0])
    h = .1
    def F(x, y):
        F = np.zeros(2)
        F[0] = y[1]
        F[1] =
        return F

    X, Y = cf.integrate(f, 0, 1, .5, .1)


